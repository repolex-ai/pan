# Test: give Pan's XMP namespace its own family-0 group so readers that
# section by family 0 (File / PNG / EXIF / XMP / IPTC ...) show "Pan".
%Image::ExifTool::UserDefined::pan = (
    GROUPS => { 0 => 'Pan', 1 => 'Pan', 2 => 'Image' },
    NAMESPACE => { 'pan' => 'https://repolex.ai/ontology/pan/' },
    WRITABLE => 'string',
    image => {
        Struct => {
            NAMESPACE => { 'pan' => 'https://repolex.ai/ontology/pan/' },
            STRUCT_NAME => 'PanImage',
            id          => { },
            mediaPath   => { },
            createdDate => { },
            mediaType   => { },
            width       => { },
            height      => { },
            readyDate   => { },
            thumbnail   => { Struct => { NAMESPACE => { 'pan' => 'https://repolex.ai/ontology/pan/' }, STRUCT_NAME => 'PanThumb', path => {}, width => {}, height => {} } },
        },
    },
);
%Image::ExifTool::UserDefined = (
    'Image::ExifTool::XMP::Main' => {
        pan => { SubDirectory => { TagTable => 'Image::ExifTool::UserDefined::pan' } },
    },
);

# copia — the Moment block Horae writes into the image. Only the namespace
# and the group are declared; ExifTool picks up every copia property in the
# file on its own, so the vocabulary can grow without touching this file.
%Image::ExifTool::UserDefined::copia = (
    GROUPS => { 0 => 'Copia', 1 => 'Copia', 2 => 'Image' },
    NAMESPACE => { 'copia' => 'https://repolex.ai/ontology/copia/' },
    WRITABLE => 'string',
);
$Image::ExifTool::UserDefined{'Image::ExifTool::XMP::Main'}{copia} =
    { SubDirectory => { TagTable => 'Image::ExifTool::UserDefined::copia' } };
1;
